spring:
  datasource:
    username: admin
    url: jdbc:h2:mem:qatester
    driverClassName: org.h2.Driver
    password: admin
  h2:
    console:
      enabled: 'true'
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
